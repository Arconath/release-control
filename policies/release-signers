# OpenSSH allowed-signers format. Exactly one real key for `hermawan22` must
# be added through a reviewed PR before any release intent can verify. Do not
# commit a placeholder key or a private key.
# policy: minimumDistinctSigners: 1
# operator: hermawan22
# identity[,identity...] [options] key-type base64-key [comment]
