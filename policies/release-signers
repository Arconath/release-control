# OpenSSH allowed-signers format. Two independent named operator keys must be
# added through reviewed PRs before any release intent can verify. Do not commit
# a placeholder key, private key, or two aliases for one key.
# policy: minimumDistinctSigners: 2
# principals: two distinct named GitHub accounts
# identity[,identity...] [options] key-type base64-key [comment]
